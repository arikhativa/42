
#include <stdio.h>
#include <string.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include "../includes/libft.h"

int main()
{
	char *a;
	
	a = ft_strnew(50);
	//a[0] = (char*)malloc(sizeof(char) * 500);
	//a[1] = (char*)malloc(sizeof(char) * 500);
	//a[1] = "aaa";	
	//printf("%s\n", a);

	//printf("%s\n", a[1]);

	return (0);
}
